export type Product = {
	_id: string;
	name: string;
	description: string;
	stock: number;
	imageSrc: string;
	unitPrice: number;
	deliveryPrice: number;
	color: string;
	updatedAt: string;
};

// export type ProductResponse = {
// 	data: Product[];
// };
